export default interface RaceStint {
    id: string,
    min_rng: number,
    max_rng: number,
    order: number,
    reliability: boolean,
    use_driver_rating: boolean,
    use_team_rating: boolean,
    use_engine_rating: boolean,
}
